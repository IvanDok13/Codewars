//Experiment

function isolateIt(arr) {
  let res = arr.map((_, i, arr) => {
    if (arr[i].length % 2 === 0) {
      let newArr = arr[i].split('');
      console.log(newArr);
      arr[i] = newArr.splice((arr[i].length / 2), 0, '|');
      console.log(newArr);

  } 
//     else {
      
//     let aver = 0, res, len;
//           aver += i;
      
//      len = arr[i].length;
//           res = aver / len;
//       arr.splice(res, 1, '|'); 
//   }
  })
  
  return newArr;
}

console.log(isolateIt(["abcd","efgh"]));
console.log(isolateIt(["abcde","fghij"]));
