let toCamelCase = (str) => {
    let result = '';
    for (let i = 0; i < str.length; i++) {
      if (str[i] === '_' || str[i] === '-') {
        result += str[i + 1].toUpperCase();
        i++;
      
      } else
        result += str[i];
    }
      return result;
};
